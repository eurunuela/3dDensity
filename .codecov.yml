codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # basic
        target: 0
        threshold: 0
    patch:
      default:
        target: 0
        threshold: 0

ignore:
  - "3dDensity/tests/"
  - "3dDensity/_version.py"
  - "3dDensity/__init__.py"
  - "3dDensity/cli/"
